"""Model-layer package for Ollama-based annotation.

Keep everything that talks to the model (prompts, schemas, parsing, retries) here.
The FastAPI server lives in `backend/` and imports from this package.
"""
